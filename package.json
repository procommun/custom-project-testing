{
  "name": "custom-project",
  "version": "1.0.0",
  "description": "A custom project for testing a static HTML page using Vite.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^5.0.0"
  }
}